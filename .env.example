# ── Supabase (Required) ───────────────────────────────────────────────────────
VITE_SUPABASE_URL=https://ghbyibsmsztcanurozyk.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ── Google Maps (Optional - needed for Taxi page only) ────────────────────────
VITE_GOOGLE_MAPS_API_KEY=

# ── Site URL (Optional - used for SEO canonical URLs) ─────────────────────────
VITE_SITE_URL=https://eydost.com

# ── TrySoro blog embed (Optional — /blog page) ───────────────────────────────
# From app.trysoro.com → Connect Your Website. Default is baked into SoroBlogEmbed.
VITE_SORO_EMBED_ID=

# ── eSIM bot API (Required for /api/esim-* server routes) ─────────────────────
# These are server-side only (used by api/*.ts Vercel functions). NEVER prefix
# with VITE_ — the key must not be exposed to the browser. Set the same values
# in Vercel Dashboard → Settings → Environment Variables.
ESIM_BOT_API_KEY=
ESIM_BOT_BASE_URL=https://bot.eydost.az

# Public / affiliate API proxy (server-side only; never prefix with VITE_)
PUBLIC_API_BASE_URL=https://bot.eydost.az
PUBLIC_API_KEY=
PUBLIC_API_AUTH_TOKEN=
PUBLIC_API_ALLOWED_ORIGINS=https://eydost.com,https://eydost.az
PUBLIC_API_ALLOWED_HOSTS=eydost.com,eydost.az

# TikTok Pixel + Events API (server-side token; never prefix with VITE_)
TIKTOK_PIXEL_ID=D914PTRC77U133LMFKFG
TIKTOK_ACCESS_TOKEN=
TIKTOK_EVENT_SECRET=

# Taxibooker affiliate integration (server-side only; never prefix with VITE_)
TAXIBOOKER_BSQD_BOT_ID=388c046c-c54f-4b56-9107-24f4ffca0600
TAXIBOOKER_BSQD_TOKEN=
TAXIBOOKER_TRIP_WEBHOOK_SECRET=
TAXI_AGENT_COMMISSION_PERCENT=5

# Server-side SMTP for admin outreach emails (never prefix with VITE_)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=info@eydost.com
SMTP_PASS=
MAIL_FROM=Ey Dost <info@eydost.com>
